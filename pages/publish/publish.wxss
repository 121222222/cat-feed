/* 发布动态页样式 */
.form-container {
  padding: 20rpx 30rpx 200rpx;
  background: #F8F8F8;
  min-height: 100vh;
}

/* 媒体类型切换 */
.media-type-switch {
  display: flex;
  background: #FFFFFF;
  border-radius: 20rpx;
  padding: 12rpx;
  margin-bottom: 20rpx;
  box-shadow: 0 4rpx 20rpx rgba(255, 186, 163, 0.08);
}

.media-type-item {
  flex: 1;
  display: flex;
  align-items: center;
  justify-content: center;
  gap: 10rpx;
  padding: 20rpx 0;
  border-radius: 16rpx;
  font-size: 28rpx;
  color: #999;
  transition: all 0.2s ease;
}

.media-type-item.active {
  background: linear-gradient(135deg, #FFBAA3, #FFD4C4);
  color: #FFFFFF;
  font-weight: 600;
}

.media-type-icon {
  font-size: 32rpx;
}

/* 照片上传区域 */
.photo-section {
  background: #FFFFFF;
  border-radius: 20rpx;
  padding: 24rpx;
  margin-bottom: 20rpx;
  box-shadow: 0 4rpx 20rpx rgba(255, 186, 163, 0.08);
}

/* 实况图头部 */
.live-photo-header {
  display: flex;
  align-items: center;
  gap: 16rpx;
  margin-bottom: 20rpx;
  padding-bottom: 16rpx;
  border-bottom: 1rpx solid #F5EDE5;
}

.live-badge {
  padding: 8rpx 20rpx;
  background: linear-gradient(135deg, #FF6B9D, #C44AFF);
  color: #FFFFFF;
  font-size: 24rpx;
  font-weight: 600;
  border-radius: 20rpx;
}

.live-desc {
  font-size: 24rpx;
  color: #999;
}

/* 实况图项目 */
.live-photo-item {
  position: relative;
}

.live-indicator {
  position: absolute;
  top: 8rpx;
  left: 8rpx;
  padding: 4rpx 12rpx;
  background: linear-gradient(135deg, #FF6B9D, #C44AFF);
  color: #FFFFFF;
  font-size: 18rpx;
  font-weight: 600;
  border-radius: 8rpx;
  letter-spacing: 1rpx;
}

.live-photo-add {
  background: linear-gradient(135deg, #FFF7F4, #FFF0F5) !important;
  border-color: #FFBAD4 !important;
}

/* 视频上传区域 */
.video-section {
  background: #FFFFFF;
  border-radius: 20rpx;
  padding: 24rpx;
  margin-bottom: 20rpx;
  box-shadow: 0 4rpx 20rpx rgba(255, 186, 163, 0.08);
}

.video-upload-area {
  width: 100%;
}

.video-preview {
  position: relative;
  width: 100%;
  border-radius: 16rpx;
  overflow: hidden;
  background: #000;
}

.video-player {
  width: 100%;
  height: 400rpx;
}

.video-delete {
  position: absolute;
  top: 16rpx;
  right: 16rpx;
  width: 56rpx;
  height: 56rpx;
  border-radius: 50%;
  background: rgba(0,0,0,0.6);
  color: #FFF;
  font-size: 36rpx;
  display: flex;
  align-items: center;
  justify-content: center;
  line-height: 1;
  z-index: 10;
}

.video-duration {
  position: absolute;
  bottom: 16rpx;
  right: 16rpx;
  padding: 8rpx 16rpx;
  border-radius: 20rpx;
  background: rgba(0,0,0,0.6);
  color: #FFF;
  font-size: 24rpx;
  z-index: 10;
}

.video-add {
  width: 100%;
  height: 300rpx;
  border-radius: 16rpx;
  border: 2rpx dashed #FFD4C4;
  background: #FFF7F4;
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  gap: 12rpx;
}

.video-add-icon {
  font-size: 64rpx;
}

.video-add-text {
  font-size: 28rpx;
  color: #FFBAA3;
  font-weight: 500;
}

.video-limit {
  font-size: 24rpx;
  color: #BBB;
}

.photo-upload-area {
  display: flex;
  flex-wrap: wrap;
  gap: 16rpx;
  width: 100%;
}

.photo-item {
  position: relative;
  width: 200rpx;
  height: 200rpx;
  border-radius: 16rpx;
  overflow: hidden;
}

.photo-img {
  width: 100%;
  height: 100%;
}

.photo-delete {
  position: absolute;
  top: 8rpx;
  right: 8rpx;
  width: 44rpx;
  height: 44rpx;
  border-radius: 50%;
  background: rgba(0,0,0,0.5);
  color: #FFF;
  font-size: 30rpx;
  display: flex;
  align-items: center;
  justify-content: center;
  line-height: 1;
}

.photo-add {
  width: 200rpx;
  height: 200rpx;
  border-radius: 16rpx;
  border: 2rpx dashed #FFD4C4;
  background: #FFF7F4;
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  gap: 8rpx;
}

.photo-add-icon {
  font-size: 48rpx;
}

.photo-add-text {
  font-size: 24rpx;
  color: #FFBAA3;
}

.photo-count {
  font-size: 20rpx;
  color: #BBB;
}

.photo-tips {
  display: flex;
  align-items: center;
  gap: 8rpx;
  margin-top: 16rpx;
  font-size: 24rpx;
  color: #999;
}

.tip-icon {
  font-size: 24rpx;
}

/* 表单区块 */
.form-section {
  background: #FFFFFF;
  border-radius: 20rpx;
  padding: 24rpx;
  margin-bottom: 20rpx;
  box-shadow: 0 4rpx 20rpx rgba(255, 186, 163, 0.08);
}

/* 标题输入 */
.title-input {
  font-size: 32rpx;
  font-weight: 600;
  color: #2D2D2D;
  width: 100%;
  padding: 10rpx 0;
}

.title-count {
  display: block;
  text-align: right;
  font-size: 22rpx;
  color: #BBB;
  margin-top: 8rpx;
}

/* 内容输入 */
.content-textarea {
  width: 100%;
  min-height: 280rpx;
  font-size: 28rpx;
  color: #2D2D2D;
  line-height: 1.7;
  box-sizing: border-box;
}

.content-count {
  display: block;
  text-align: right;
  font-size: 22rpx;
  color: #BBB;
  margin-top: 8rpx;
}

/* 区块标题 */
.section-header {
  display: flex;
  align-items: center;
  gap: 8rpx;
  margin-bottom: 20rpx;
}

.section-icon {
  font-size: 28rpx;
}

.section-title {
  font-size: 28rpx;
  font-weight: 600;
  color: #2D2D2D;
}

.section-opt {
  font-size: 24rpx;
  color: #BBB;
}

/* 分类选项 */
.category-options {
  display: flex;
  flex-wrap: wrap;
  gap: 16rpx;
}

.category-option {
  padding: 14rpx 28rpx;
  border-radius: 30rpx;
  background: #F5F5F5;
  font-size: 26rpx;
  color: #666;
  border: 2rpx solid transparent;
  transition: all 0.2s ease;
}

.category-option.active {
  background: #FFEFE9;
  color: #FFBAA3;
  border-color: #FFBAA3;
}

/* 猫咪选项 */
.cat-options {
  display: flex;
  flex-wrap: wrap;
  gap: 16rpx;
}

.cat-option {
  display: flex;
  align-items: center;
  gap: 10rpx;
  padding: 12rpx 24rpx;
  border-radius: 30rpx;
  background: #F5F5F5;
  font-size: 26rpx;
  color: #666;
  border: 2rpx solid transparent;
}

.cat-option.active {
  background: #FFEFE9;
  color: #FFBAA3;
  border-color: #FFBAA3;
}

.cat-breed-tag {
  font-size: 20rpx;
  color: #999;
  padding: 4rpx 12rpx;
  background: #F0F0F0;
  border-radius: 16rpx;
  margin-left: 4rpx;
}

.cat-option.active .cat-breed-tag {
  background: rgba(255, 186, 163, 0.3);
  color: #FFBAA3;
}

.cat-avatar {
  width: 48rpx;
  height: 48rpx;
  border-radius: 50%;
  background: #FFD4C4;
}

.no-cat-tip {
  font-size: 26rpx;
  color: #999;
}

.no-cat-tip .link {
  color: #FFBAA3;
  text-decoration: underline;
}

/* 话题标签 */
.topic-options {
  display: flex;
  flex-wrap: wrap;
  gap: 16rpx;
}

.topic-tag {
  padding: 12rpx 24rpx;
  border-radius: 30rpx;
  background: #F5F5F5;
  font-size: 24rpx;
  color: #666;
  border: 2rpx solid transparent;
}

.topic-tag.active {
  background: #E8F4FD;
  color: #1890FF;
  border-color: #91D5FF;
}

.topic-tag.topic-add {
  background: #FFF7F4;
  color: #FFBAA3;
  border: 2rpx dashed #FFD4C4;
}

/* 自定义话题输入 */
.custom-topic-input {
  display: flex;
  align-items: center;
  gap: 16rpx;
  margin-top: 20rpx;
  padding-top: 20rpx;
  border-top: 1rpx solid #F5EDE5;
}

.topic-input {
  flex: 1;
  height: 72rpx;
  padding: 0 24rpx;
  background: #F8F8F8;
  border-radius: 36rpx;
  font-size: 26rpx;
  color: #333;
}

.topic-confirm-btn {
  padding: 16rpx 32rpx;
  background: linear-gradient(135deg, #FFBAA3, #FFD4C4);
  color: #FFFFFF;
  font-size: 26rpx;
  font-weight: 500;
  border-radius: 36rpx;
}

/* 提交栏 */
.submit-bar {
  position: fixed;
  bottom: 0;
  left: 0;
  right: 0;
  padding: 20rpx 30rpx;
  background: #FFFFFF;
  box-shadow: 0 -2rpx 20rpx rgba(0,0,0,0.05);
  z-index: 100;
}

.submit-row {
  display: flex;
  gap: 20rpx;
}

.btn-draft {
  flex: 1;
  height: 88rpx;
  border-radius: 44rpx;
  background: #F5F5F5;
  color: #666;
  font-size: 30rpx;
  display: flex;
  align-items: center;
  justify-content: center;
}

.btn-publish {
  flex: 2;
  height: 88rpx;
  border-radius: 44rpx;
  background: linear-gradient(135deg, #FF8A6B, #FF6B4A);
  color: #FFFFFF;
  font-size: 30rpx;
  font-weight: 600;
  display: flex;
  align-items: center;
  justify-content: center;
}

.btn-publish:active {
  opacity: 0.85;
}

/* 可见范围选择 */
.visibility-options {
  display: flex;
  flex-direction: column;
  gap: 16rpx;
}

.visibility-option {
  display: flex;
  align-items: center;
  padding: 24rpx;
  border-radius: 16rpx;
  background: #F8F8F8;
  border: 2rpx solid transparent;
  transition: all 0.2s ease;
}

.visibility-option.active {
  background: #FFF7F4;
  border-color: #FFBAA3;
}

.visibility-icon {
  font-size: 36rpx;
  margin-right: 20rpx;
}

.visibility-info {
  flex: 1;
  display: flex;
  flex-direction: column;
  gap: 4rpx;
}

.visibility-name {
  font-size: 28rpx;
  font-weight: 600;
  color: #2D2D2D;
}

.visibility-desc {
  font-size: 22rpx;
  color: #999;
}

.visibility-check {
  width: 40rpx;
  height: 40rpx;
  border-radius: 50%;
  background: #FFBAA3;
  color: #FFFFFF;
  font-size: 24rpx;
  display: flex;
  align-items: center;
  justify-content: center;
}

/* 视频封面设置 */
.video-cover-section {
  margin-top: 24rpx;
  padding-top: 24rpx;
  border-top: 1rpx solid #F5EDE5;
}

.video-cover-area {
  margin-top: 16rpx;
}

.video-cover-preview {
  position: relative;
  width: 200rpx;
  height: 200rpx;
  border-radius: 16rpx;
  overflow: hidden;
}

.cover-img {
  width: 100%;
  height: 100%;
}

.cover-change {
  position: absolute;
  bottom: 0;
  left: 0;
  right: 0;
  padding: 12rpx 0;
  background: rgba(0,0,0,0.6);
  color: #FFF;
  font-size: 22rpx;
  text-align: center;
}

.video-cover-add {
  width: 200rpx;
  height: 200rpx;
  border-radius: 16rpx;
  border: 2rpx dashed #FFD4C4;
  background: #FFF7F4;
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  gap: 10rpx;
}

.cover-add-icon {
  font-size: 48rpx;
  color: #FFBAA3;
}

.cover-add-text {
  font-size: 24rpx;
  color: #FFBAA3;
}
